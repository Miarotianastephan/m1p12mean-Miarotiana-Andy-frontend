export interface Parts {
  brand: string;
  brandId: string;
  description: string;
  model: string;
  modelId: string;
  partName: string;
  price: number;
  _id: string;
  quantite_devis: number; // seulement pour le generation devis
}
